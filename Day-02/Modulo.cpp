#include <iostream>
using namespace std;
int main(){
    cout<<1234445%4;
}

//if a>b => a%b -> remainder
//if a<b  => a%b = a ( divisible is not possible thus :) ) 
//if a%(-b) -> a%b
//if -a%b = -(a%b)
//if -a%-b = -->-(a%b)  ( reason  first let -b = alpha now we will get 
// -(a%alpha) we know alpha = -b so now the reult would be -(a%b))

